[tool.pytest.ini_options]
testpaths = ["tests"]
markers = [
    "slow: marks tests as slow (deselect with '-m \"not slow\"')",
    "api: marks tests that require real API access",
    "integration: marks integration tests",
]
addopts = "-v --tb=short"
